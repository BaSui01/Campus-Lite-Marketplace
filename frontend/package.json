{
  "name": "@campus/root",
  "version": "1.0.0",
  "description": "校园轻享集市系统 - Monorepo 前端项目",
  "author": "BaSui 😎",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:admin": "pnpm --filter @campus/admin dev",
    "dev:portal": "pnpm --filter @campus/portal dev",
    "build:admin": "pnpm --filter @campus/admin build",
    "build:portal": "pnpm --filter @campus/portal build",
    "build:shared": "pnpm --filter @campus/shared build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "api:generate": "node ../scripts/generate-openapi.js",
    "api:clean": "rm -rf packages/shared/src/api/models && rm -rf packages/shared/src/api/api && rm packages/shared/src/api/*.d.ts",
    "api:watch": "node ../scripts/watch-api.js",
    "api:watch:bash": "chmod +x ../scripts/watch-api.sh && ../scripts/watch-api.sh",
    "format": "prettier --write \"packages/**/*.{ts,tsx,js,jsx,json,css,scss,md}\"",
    "format:check": "prettier --check \"packages/**/*.{ts,tsx,js,jsx,json,css,scss,md}\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0",
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,css,scss,md}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "echarts-for-react": "^3.0.5",
    "react-toastify": "^11.0.5"
  }
}
