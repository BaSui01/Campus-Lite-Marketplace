# 开发环境配置

# API 基础 URL（✅ 必须加 /api 后缀，因为后端 context-path=/api）
VITE_API_BASE_URL=http://localhost:8200/api

# 密码加密密钥（AES-256，32字节，Base64编码）
# ⚠️ 注意：此密钥必须与后端配置保持一致
VITE_ENCRYPT_KEY=JMMgf1RSDLBNksxUSNKl/2q94N6cIY7voJRXqlzcCBA=
